[package]
name = "window-layout-manager"
version = "0.1.0"
authors = ["Erich Schroeter <erichschroeter@gmail.com>"]
edition = "2018"

[dependencies]

[target.'cfg(windows)'.dependencies]
winapi = { version = "0.3", features = ["winuser", "processthreadsapi", "libloaderapi", "handleapi"] }